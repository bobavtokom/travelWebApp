
<% 
GetTopLikedDestination = function(n,el) {
  const url = `http://localhost:8080/topLiked/${n}`;
  fetch(url, {
    method: 'GET',
  })
  .then(res => {
    if(res.ok) { 
        console.log(el);
        console.log(document.querySelector(`#{el}`));
        return res;
    } else {
      console.error('functions.ejs: else: Error getting top liked destination', res.status);
    }
  })
  .catch(err => {
    console.error('functions.ejs: catch: Error getting top liked destination', err);
  });
}

PrintSomething = function(text) {
  console.log(text);
}
%>